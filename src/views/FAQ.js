import React from 'react';
import { Link } from 'react-router-dom';

const FAQ = () => {
    return (
        <>
        FAQ: None, because no one has ever asked us a question more than once
         </>
    );
};

export default FAQ;